import Head from "next/head";

type Props = {};

const HeadTitle = (_props: Props): JSX.Element => {
  return (
    <Head>
      <title>drumato.com</title>
    </Head>
  );
};

export default HeadTitle;
